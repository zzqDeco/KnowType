# CLAUDE.md

Follow `AGENTS.md` as the authoritative repository guide.

Project-specific reminders:

- Do not turn continuation candidates into full-sentence rewrites.
- Keep provider-specific code inside `KnowTypeProviders`.
- Keep user-visible Chinese product docs in `README_CN.md`; keep code comments and identifiers in English.
- Run `swift test` after code changes.
